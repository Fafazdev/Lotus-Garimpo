spring.application.name=LotusGarimpo
spring.thymeleaf.cache=false

# --- CONFIGURAÇÃO DO BANCO DE DADOS ---
# Corrigido: 'spring' (estava 'pring') e o nome do banco para 'lotus_garimpo'
spring.datasource.url=jdbc:mysql://localhost:3306/lotus_db?useTimezone=true&serverTimezone=UTC
# Login do seu phpMyAdmin (Geralmente root / vazio)
spring.datasource.username=root
spring.datasource.password=

# Driver de conexão
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do Hibernate (JPA)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=9090

# Força o Hibernate a criar as tabelas baseadas nas classes @Entity
# (mantido configuração para MySQL)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# NOTA: usando dialect MySQL nas configurações anteriores


# ==========================
# Configuração de frete (Correios)
# ==========================
# CEP de origem da loja (apenas números)
lotus.frete.cep-origem=11010-970
# Código de serviço dos Correios (ex.: 04510 PAC, 04014 SEDEX)
lotus.frete.codigo-servico=04510


# ==========================
# Importa configurações locais (opcional)
# ==========================
# Se existir o arquivo application-oauth.properties, ele será lido também.
spring.config.import=optional:classpath:application-oauth.properties


# ==========================
# (Removido) OAuth2 - Login com Google
# As propriedades abaixo foram desativadas para não exigir configuração
# de OAuth2/Google em outras máquinas.
# ==========================
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
# spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
# spring.security.oauth2.client.registration.google.client-name=Google
# spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code
# spring.security.oauth2.client.registration.google.client-authentication-method=client_secret_basic


# ==========================
# Mercado Pago - Credenciais de teste (preencher manualmente)
# ==========================
# NÃO coloque o token direto no código público / Git.
# Preencha esses valores apenas no seu ambiente local.
mercadopago.public-key=TEST-e952f3a8-0727-41f0-a38a-733ec01f6533
mercadopago.access-token=TEST-2541425494090874-030918-c99b71e36c313079c4d794878f7ca91a-305304828
